version: '3.8'

services:
  flexicaf:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: flexGant_app
    restart: unless-stopped
    ports:
      - "7020:80" 
    volumes:
      - ./src/db:/var/www/html/db
